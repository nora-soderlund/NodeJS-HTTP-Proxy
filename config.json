{
    "port": 80,
    
    "rules": {
        "allowed-hosts": [ "nora-soderlund.se", "ridetracker.app", "localhost" ]
    },

    "routes": [
        {
            "origin": "localhost",
            "target": "http://localhost:81"
        },
        
        {
            "origin": "api.localhost",
            "target": "http://localhost:82"
        }
    ]
}
